# APK Manager - Docker Environment Variables
# Používá se pro docker-compose.yml

# Application Ports
APP_PORT=8580
API_PORT=3000
TZ=Europe/Prague

# APK Storage Path
# Cesta k hlavnímu adresáři pro APK soubory na hostitelském systému
# Adresáře staging, development, release-candidate, production budou vytvořeny automaticky
APK_DATA_PATH=/files/docker/apk-manager

# Database Configuration
POSTGRES_USER=apkmanager
POSTGRES_PASSWORD=ZMĚŇ-TOTO-NA-SILNÉ-HESLO
POSTGRES_DB=apkmanager
POSTGRES_PORT=5432

# JWT Authentication
JWT_SECRET=ZMĚŇ-TOTO-NA-NÁHODNÝ-SECRET-MIN-32-ZNAKŮ

# =======================================
# KRITICKÉ BEZPEČNOSTNÍ NASTAVENÍ
# =======================================

# 1. POSTGRES_PASSWORD
#    Použij silné heslo (min 16 znaků)
#    Generuj pomocí: openssl rand -base64 24
#    Příklad: openssl rand -base64 24
#    Výstup: aBc123XyZ456...

# 2. JWT_SECRET
#    Použij náhodný secret (min 32 znaků)
#    Generuj pomocí: openssl rand -hex 32
#    Příklad: openssl rand -hex 32
#    Výstup: 1a2b3c4d...

# 3. APK_DATA_PATH
#    Ujisti se že adresář existuje a má správná oprávnění:
#    mkdir -p /files/docker/apk-manager/{staging,development,release-candidate,production}
#    chmod -R 755 /files/docker/apk-manager
#    chown -R 1000:1000 /files/docker/apk-manager

# =======================================
# POZNÁMKY K NASAZENÍ
# =======================================

# APK adresáře v kontejneru:
# - Backend používá výchozí cestu /data/apk
# - APK_DATA_PATH se mountuje do /data/apk v kontejneru
# - Administrátoři mohou změnit strukturu adresářů přes UI (Settings)
# - Změny v Settings se ukládají do databáze
